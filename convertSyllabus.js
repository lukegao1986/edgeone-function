const fs = require('fs');

// The original content string is parsed manually or we just use require after modifying it
// But it's easier to just use typescript to transpile or we can write a small ts-node script
